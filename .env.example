# Convex
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# CORS Configuration
# Comma-separated list of allowed origins for API requests
# Leave empty or set to '*' for development (allows all origins)
# Set to specific domains in production for security
# Example: ALLOWED_ORIGINS=https://dashboard.example.com,https://app.example.com
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# Better Auth
# Required in production. When omitted in development, GitHub OAuth is disabled
# gracefully — the login button is hidden and no warning is logged.
# Create a GitHub OAuth App at https://github.com/settings/developers and set:
#   Homepage URL:    http://localhost:3000
#   Callback URL:    http://localhost:3000/api/auth/callback/github
BETTER_AUTH_SECRET=your-random-secret-at-least-32-chars
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# App URL (used by Better Auth for redirect URLs)
PUBLIC_APP_URL=http://localhost:3000
BETTER_AUTH_URL=http://localhost:3000

#!/usr/bin/env bash
# Delegates to the repo-managed pre-push check script.
# Install with: bash scripts/install-hooks.sh
set -euo pipefail
exec "$(git rev-parse --show-toplevel)/scripts/pre-push-check.sh"
